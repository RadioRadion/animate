<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.2.4/gsap.min.js"></script>
    <script
        src="https://code.jquery.com/jquery-3.4.1.min.js"
        integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo="
        crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/fullPage.js/3.0.5/fullpage.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300;0,400;0,600;1,300;1,400;1,600&display=swap" rel="stylesheet">
    <title>TODO</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>

  <body>
    <%= yield %>
    <div class="cursor"></div>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
    <%= javascript_pack_tag 'cursor' %>
    <%= javascript_pack_tag 'fullpage' %>
    <%= javascript_pack_tag 'gallerie' %>
    <%= javascript_pack_tag 'terminal' %>


<!--     //le script à lancer en fin de page
 -->
    <script type="text/javascript">
            var myFullpage = new fullpage('#fullpage', {
        sectionsColor: ['#1bbc9b', '#4BBFC3', '#7BAABE', 'whitesmoke', '#ccddff'],
        anchors: ['firstPage', 'secondPage', '3rdPage', '4thpage', 'lastPage'],
        navigation:true,
        navigationTooltips: ['', 'f l a s h', 't v'],
        showActiveTooltip: true,
        loopBottom: true,
        scrollingSpeed: 1700,
        menu: '#menu',
        licenseKey: '7D72BD02-D9324668-A20A7371-BA347154',
        responsiveWidth: 900,
        afterResponsive: function(isResponsive){

        },
        onLeave: function(origin, destination, direction){
            var params = {
                origin: origin,
                destination:destination,
                direction: direction
            };
            if (params.destination.anchor === "secondPage") {
              gsap.from(".fp-prev", {duration: 6, x: -100, opacity: 0, scale: 0.5});
              gsap.from(".fp-next", {duration: 6, x: 100, opacity: 0, scale: 0.5});
            }
            if (params.destination.anchor === "3rdPage") {
              gsap.from(".fp-prev", {duration: 6, x: -100, opacity: 0, scale: 0.5});
              gsap.from(".fp-next", {duration: 6, x: 100, opacity: 0, scale: 0.5});
            }
            const terminal = document.querySelector('#AboutDevTypeText');
            terminal.innerHTML = "";
        },
        onSlideLeave: function(section, origin, destination, direction){
            var params = {
                section: section,
                origin: origin,
                destination: destination,
                direction: direction
            };

        },
        afterRender: function(){
        },
        afterResize: function(width, height){
        },
        afterSlideLoad: function(section, origin, destination, direction){
            var params = {
                section: section,
                origin: origin,
                destination: destination,
                direction: direction
            };
        },
        afterLoad: function(origin, destination, direction){
            var params = {
                origin: origin,
                destination: destination,
                direction: direction
            };
        }
    });
    </script>

  </body>
</html>
